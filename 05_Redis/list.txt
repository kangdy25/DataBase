# 왼쪽(앞쪽)에 값 삽입
LPUSH l 1
LPUSH l 2
LPUSH l 3
LPUSH l 4
LPUSH l 5

# 오른쪽(뒤쪽)에 값 삽입
RPUSH l 0
RPUSH l -1
RPUSH l -2
RPUSH l -3
RPUSH l -4
RPUSH l -5

# 왼쪽(앞쪽)에서 값 꺼내기
LPOP l

# 오른쪽(뒤쪽)에서 값 꺼내기
RPOP l

# 리스트 길이 확인
LLEN l

# 리스트 전체 조회 (0부터 끝까지)
LRANGE l 0 -1
